language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t adelaboufakher/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true adelaboufakher/docker-react npm run test
